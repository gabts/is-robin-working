{
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development nodemon build/main.js --ignore *.json",
    "start": "NODE_ENV=production node build/main.js",
    "test": "mocha --exit -t 20000 build/**/*.spec.js"
  },
  "dependencies": {
    "discord.js": "^14.11.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.4"
  }
}
